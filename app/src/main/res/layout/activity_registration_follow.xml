<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                tools:context="adoptplanet.com.adoptplanet.view.RegistrationFollowActivity">

<android.support.v7.widget.Toolbar
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/registration_follow_toolbar"
    android:background="@color/blue">

</android.support.v7.widget.Toolbar>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Continue"
        android:id="@+id/reg_follow_continue_b"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:background="@color/orange"
        android:textAllCaps="false"
        android:onClick="handleNextActivity"
        android:textColor="@color/white"/>

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/reg_follow_listview"
        android:layout_below="@+id/registration_follow_toolbar"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/reg_follow_continue_b"/>

</RelativeLayout>
